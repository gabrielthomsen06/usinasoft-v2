services:
  # ─── Caddy (Reverse Proxy + HTTPS automático) ───
  caddy:
    image: caddy:2-alpine
    container_name: usinasoft_caddy
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - DOMAIN=${DOMAIN:?DOMAIN is required in .env}
      - SSL_EMAIL=${SSL_EMAIL:?SSL_EMAIL is required in .env (Let's Encrypt)}
    depends_on:
      - frontend
    networks:
      - usinasoft_network
    restart: unless-stopped

  # ─── Banco de Dados ───
  db:
    image: postgres:15-alpine
    container_name: usinasoft_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required in .env}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB is required in .env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - usinasoft_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # ─── Backend (FastAPI) ───
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: usinasoft_backend
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      - SECRET_KEY=${SECRET_KEY:?SECRET_KEY is required in .env (min 32 chars)}
      - CORS_ORIGINS=["https://${DOMAIN}","http://${DOMAIN}"]
      - DEBUG=${DEBUG:-false}
      - EMPRESA_CNPJ=${EMPRESA_CNPJ:?EMPRESA_CNPJ is required in .env (14 digits)}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - usinasoft_network
    restart: unless-stopped

  # ─── Frontend (Nginx + React) ───
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: usinasoft_frontend
    depends_on:
      - backend
    networks:
      - usinasoft_network
    restart: unless-stopped

volumes:
  postgres_data:
  caddy_data:
  caddy_config:

networks:
  usinasoft_network:
    driver: bridge
